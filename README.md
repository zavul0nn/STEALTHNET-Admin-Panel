# StealthNET Admin Panel

Панель управления для VPN панели Remnawave с поддержкой тарифов, платежей, реферальной программы и системы поддержки. А также интеграцией bedolaga-bot



<img width="2037" height="1019" alt="image" src="https://github.com/user-attachments/assets/521f0bce-5d88-4da4-a655-f8535c63fcc6" />

## 🧪 [Тестирование сайта](https://panel.stealthnet.app/login)

## 💬 **[STEALTHNET Chat](https://t.me/stealthnet_admin_panel)** - Для общения, вопросов, предложений
## 🚀 Возможности

- **Управление пользователями** - регистрация, верификация, управление ролями
- **Тарифы** - создание и управление тарифными планами с поддержкой разных валют
- **Платежи** - интеграция с CrystalPay, Heleket и Telegram Stars
- **Промокоды** - система промокодов с поддержкой скидок и бесплатных дней
- **Реферальная программа** - настройка бонусов за приглашения
- **Поддержка** - система тикетов для связи с пользователями
- **Многоязычность** - поддержка русского, украинского, английского и китайского языков
- **Email рассылки** - массовая рассылка сообщений пользователям

## 📋 Требования

- Python 3.11+
- Node.js 18+
- Docker и Docker Compose (для Docker развертывания)

## 🛠️ Установка

### Вариант 1: Docker (Рекомендуется)

1. Клонируйте репозиторий:
```bash
git clone https://github.com/GOFONCK/stealthnet-admin-panel.git
cd stealthnet-admin-panel
```

2. Создайте файл `.env` на основе `.env.example`:
```bash
cp .env.example .env
```

3. Заполните переменные окружения в `.env`:
```env
ADMIN_TOKEN=your_admin_token
API_URL=https://api.example.com
DEFAULT_SQUAD_ID=your_squad_id
YOUR_SERVER_IP=https://panel.yourdomain.com
FERNET_KEY=your_fernet_key
JWT_SECRET_KEY=your_jwt_secret
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=465
MAIL_USERNAME=your_email@gmail.com
MAIL_PASSWORD=your_app_password
```

4. Запустите через Docker Compose:
```bash
docker-compose up -d
```

5. Инициализируйте базу данных:
```bash
docker-compose exec backend python3 init_db.py
```

6. Создайте администратора:
```bash
docker-compose exec backend python3 -m flask --app app make-admin your@email.com
```

Приложение будет доступно:
- Frontend: http://localhost
- Backend API: http://localhost:5000

### Вариант 2: Локальная установка

#### Backend

1. Создайте виртуальное окружение:
```bash
python3 -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows
```

2. Установите зависимости:
```bash
pip install -r requirements.txt
```

3. Создайте `.env` файл (см. `.env.example`)

4. Инициализируйте базу данных:
```bash
python3 init_db.py
```

5. Создайте администратора:
```bash
python3 -m flask --app app make-admin your@email.com
```

6. Запустите сервер:
```bash
# Для разработки
python3 app.py

# Для продакшена
gunicorn --workers 3 --bind 127.0.0.1:5000 app:app
```

#### Frontend

1. Перейдите в директорию admin-panel:
```bash
cd admin-panel
```

2. Установите зависимости:
```bash
npm install --legacy-peer-deps
```

3. Запустите в режиме разработки:
```bash
npm start
```

4. Для продакшена соберите проект:
```bash
npm run build
```

## 📁 Структура проекта

```
.
├── app.py                 # Flask приложение (backend)
├── init_db.py            # Скрипт инициализации БД
├── requirements.txt      # Python зависимости
├── Dockerfile            # Docker для backend
├── docker-compose.yml    # Docker Compose конфигурация
├── .env.example         # Пример переменных окружения
├── templates/           # HTML шаблоны для email
├── instance/            # База данных и кэш (создается автоматически)
└── admin-panel/         # React приложение (frontend)
    ├── src/
    │   ├── pages/       # Страницы приложения
    │   ├── components/  # React компоненты
    │   └── ...
    ├── Dockerfile       # Docker для frontend
    └── package.json
```

## 🔧 Конфигурация

### Переменные окружения

Все настройки приложения задаются через переменные окружения в файле `.env`:

- `ADMIN_TOKEN` - токен администратора для внешнего API
- `API_URL` - URL внешнего API VPN провайдера
- `DEFAULT_SQUAD_ID` - ID дефолтного сквада
- `YOUR_SERVER_IP` - URL вашего сервера (для CORS и ссылок)
- `FERNET_KEY` - ключ для шифрования (сгенерируйте через Python)
- `JWT_SECRET_KEY` - секретный ключ для JWT токенов
- `MAIL_*` - настройки почтового сервера

### Генерация FERNET_KEY

```bash
python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
```

## 🔐 Безопасность

- Все пароли хешируются с помощью bcrypt
- API ключи шифруются с помощью Fernet
- JWT токены для аутентификации
- Rate limiting для защиты от спама
- CORS настроен для безопасности

## 📝 API Документация

### Публичные эндпоинты

- `POST /api/public/register` - Регистрация пользователя
- `POST /api/public/login` - Вход в систему
- `GET /api/public/tariffs` - Список тарифов
- `POST /api/public/verify-email` - Верификация email

### Клиентские эндпоинты (требуют авторизации)

- `GET /api/client/me` - Информация о текущем пользователе
- `GET /api/client/nodes` - Список доступных серверов
- `POST /api/client/create-payment` - Создание платежа
- `POST /api/client/activate-trial` - Активация триала

### Админские эндпоинты (требуют роль ADMIN)

- `GET /api/admin/users` - Список всех пользователей
- `GET /api/admin/tariffs` - Управление тарифами
- `GET /api/admin/promocodes` - Управление промокодами
- `POST /api/admin/broadcast` - Email рассылка

## 🐳 Docker команды

```bash
# Запуск
docker-compose up -d

# Остановка
docker-compose down

# Просмотр логов
docker-compose logs -f

# Пересборка после изменений
docker-compose up -d --build

# Выполнение команд в контейнере
docker-compose exec backend python3 -m flask --app app make-admin email@example.com
```

## 🧪 Разработка

### Запуск в режиме разработки

Backend:
```bash
python3 app.py
```

Frontend:
```bash
cd admin-panel
npm start
```

### Тестирование

```bash
# Backend тесты (если есть)
pytest

# Frontend тесты
cd admin-panel
npm test
```

## 📄 Лицензия

Этот проект распространяется под лицензией MIT. См. файл LICENSE для подробностей.

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для новой функции (`git checkout -b feature/AmazingFeature`)
3. Закоммитьте изменения (`git commit -m 'Add some AmazingFeature'`)
4. Запушьте в ветку (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📞 Поддержка

Если у вас возникли вопросы или проблемы, создайте Issue в репозитории.

## 🔄 Обновление

При обновлении проекта:

1. Остановите контейнеры: `docker-compose down`
2. Получите последние изменения: `git pull`
3. Пересоберите образы: `docker-compose up -d --build`
4. При необходимости выполните миграции БД

---

**Примечание**: Убедитесь, что все переменные окружения правильно настроены перед запуском в продакшене!

💰 Наши реквизиты для поддержки (Криптовалюта):
Мы принимаем донаты в криптовалюте. Пожалуйста, убедитесь, что вы отправляете средства в подходящей сети для каждого адреса.

Адрес для (TRX/USDT-TRC20 и аналогичных): TWG5eK5Yc4BRHhaMc1GncPtj2pP3mfJ7ag

Адрес для (Ethereum / BSC / EVM-совместимые сети): 0x7cfD59365ee3C97576fe387886b55298c9CeAc5E

Адрес для (Ethereum / BSC / EVM-совместимые сети): 0x23cB77312DC0163d80d8bf23df272aCD21177846

Либо  звездами тг ⭐️
Огромное вам спасибо за вашу веру в наш проект! Вместе мы сильнее! 💪
