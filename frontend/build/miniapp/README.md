# StealthNET MiniApp - Кабинет подписки

Telegram Web App для отображения информации о подписке пользователя.

## Описание

Мини-апп предоставляет удобный интерфейс для просмотра:
- Статуса подписки
- Ссылки подключения к VPN
- Данных для входа на сайте (логин и пароль)
- Информации о тарифе
- Списка доступных серверов
- Использования трафика

## Установка

### 1. Разместите файлы

Скопируйте папку `miniapp` на ваш веб-сервер, чтобы файлы были доступны по HTTPS.

Например:
```
/var/www/miniapp/
  └── index.html
```

### 2. Настройте доступ

Убедитесь, что файлы доступны по HTTPS URL, например:
- `https://panel.stealthnet.app/miniapp/`
- `https://miniapp.stealthnet.app/`

### 3. Настройте в BotFather

1. Откройте [@BotFather](https://t.me/BotFather)
2. Выберите вашего бота
3. Отправьте `/newapp`
4. Выберите бота
5. Укажите название мини-аппа
6. Загрузите иконку (опционально)
7. Укажите URL: `https://panel.stealthnet.app/miniapp/`
8. Укажите короткое описание

### 4. Обновите переменную окружения

В `.env` файле бота установите:
```env
MINIAPP_URL=https://panel.stealthnet.app/miniapp/
```

### 5. Настройте CORS (если нужно)

Убедитесь, что ваш Flask API разрешает запросы с домена мини-аппа.

В `app.py`:
```python
CORS(app, resources={
    r"/api/.*": {
        "origins": [
            "http://localhost:3000",
            YOUR_SERVER_IP_OR_DOMAIN,
            "https://panel.stealthnet.app",  # Добавьте домен мини-аппа
            "https://web.telegram.org"  # Telegram Web App
        ]
    }
})
```

## Использование

После настройки пользователи смогут открыть мини-апп через кнопку "📱 Кабинет" в боте.

Мини-апп автоматически:
1. Получает токен авторизации через `telegram_id`
2. Загружает данные пользователя
3. Отображает информацию о подписке

## API Эндпоинты

Мини-апп использует следующие эндпоинты:

1. **`POST /api/bot/get-token`** - Получение JWT токена по `telegram_id`
2. **`GET /api/client/me`** - Получение данных пользователя
3. **`POST /api/bot/get-credentials`** - Получение логина и пароля

## Безопасность

- Мини-апп работает только внутри Telegram
- Авторизация происходит через `telegram_id` из Telegram Web App
- Все запросы идут через HTTPS
- Пароль отображается только если он сохранен в зашифрованном виде

## Кастомизация

Вы можете изменить стили и функциональность, отредактировав `index.html`:
- Цвета и стили в секции `<style>`
- Логику загрузки данных в секции `<script>`
- Структуру отображения в секции `<body>`

## Поддержка

При возникновении проблем проверьте:
1. Файлы доступны по HTTPS
2. CORS настроен правильно
3. API эндпоинты работают
4. URL в BotFather совпадает с реальным URL

