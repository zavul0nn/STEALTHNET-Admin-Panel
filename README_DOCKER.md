# 🐳 Docker Deployment Guide для StealthNET

Полное руководство по развертыванию StealthNET с использованием Docker, Nginx и Caddy.

## 📦 Что включено

- ✅ Dockerfile для Flask API
- ✅ docker-compose.yml для оркестрации
- ✅ Конфигурация Nginx
- ✅ Конфигурация Caddy
- ✅ Автоматическая инициализация БД
- ✅ Готовый фронтенд (React build)

## 🚀 Быстрая установка

### Шаг 1: Подготовка

```bash
cd "Готовый проект"

# Создайте .env файл
cp .env.example .env

# Отредактируйте .env
nano .env
```

### Шаг 2: Запуск

**Вариант A: С Nginx (рекомендуется для продакшена)**

```bash
docker-compose up -d
```

**Вариант B: С Caddy (автоматический SSL)**

```bash
docker-compose --profile caddy up -d caddy api
```

### Шаг 3: Проверка

```bash
# Проверьте статус
docker-compose ps

# Проверьте логи
docker-compose logs -f

# Откройте в браузере
# http://your-server-ip
```

## 🔧 Детальная настройка

См. файл [DOCKER_SETUP.md](./DOCKER_SETUP.md) для подробных инструкций.

## 📋 Основные команды

```bash
# Запуск
docker-compose up -d

# Остановка
docker-compose down

# Просмотр логов
docker-compose logs -f

# Перезапуск
docker-compose restart

# Обновление
docker-compose pull && docker-compose up -d
```

## 🌐 Настройка домена

### С Nginx

1. Отредактируйте `nginx.conf`
2. Замените `server_name _;` на ваш домен
3. Настройте SSL (см. DOCKER_SETUP.md)

### С Caddy

1. Отредактируйте `Caddyfile`
2. Замените `your-domain.com` на ваш домен
3. Caddy автоматически получит SSL сертификат

## 🔐 Безопасность

- ✅ API доступен только через reverse proxy
- ✅ Автоматический SSL с Caddy
- ✅ Security headers настроены
- ✅ Gzip compression включен

## 📚 Документация

- [DOCKER_SETUP.md](./DOCKER_SETUP.md) - Подробная инструкция
- [README.md](./README.md) - Общая документация проекта

