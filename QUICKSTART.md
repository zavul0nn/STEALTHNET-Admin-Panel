# 🚀 Быстрый старт StealthNET с Docker

## Минимальные требования

- Docker 20.10+
- Docker Compose 2.0+
- 2GB RAM
- 5GB свободного места

## ⚡ За 3 шага

### 1. Настройка переменных окружения

```bash
# Скопируйте пример файла
cp env.example .env

# Отредактируйте .env и заполните обязательные переменные
nano .env
```

**Обязательные переменные:**
- `ADMIN_TOKEN` - токен RemnaWave API
- `API_URL` - URL RemnaWave API
- `DEFAULT_SQUAD_ID` - ID дефолтного сквада
- `YOUR_SERVER_IP` - ваш домен (например, `https://panel.stealthnet.app`)
- `JWT_SECRET_KEY` - секретный ключ (сгенерируйте случайную строку)
- `FERNET_KEY` - ключ шифрования (сгенерируйте: `python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"`)

### 2. Запуск

**С Nginx:**
```bash
docker-compose up -d
```

**С Caddy (автоматический SSL):**
```bash
docker-compose --profile caddy up -d api caddy
```

### 3. Проверка

```bash
# Проверьте статус
docker-compose ps

# Откройте в браузере
# http://your-server-ip
```

## 📝 Создание администратора

```bash
# Войдите в контейнер
docker-compose exec api bash

# Создайте администратора
python -m flask --app app make-admin your-email@example.com

# Выйдите
exit
```

## 🔧 Настройка домена

### С Nginx

1. Отредактируйте `nginx.conf`
2. Замените `server_name _;` на ваш домен
3. Настройте SSL (см. DOCKER_SETUP.md)

### С Caddy

1. Отредактируйте `Caddyfile`
2. Раскомментируйте блок с доменом
3. Замените `your-domain.com` на ваш домен
4. Caddy автоматически получит SSL сертификат

## 📚 Полная документация

См. [DOCKER_SETUP.md](./DOCKER_SETUP.md) для подробных инструкций.

## 🆘 Проблемы?

```bash
# Просмотр логов
docker-compose logs -f

# Перезапуск
docker-compose restart

# Полная пересборка
docker-compose down
docker-compose build --no-cache
docker-compose up -d
```

