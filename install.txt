═══════════════════════════════════════════════════════════════════════════════
                         РУКОВОДСТВО ПО РАЗВЕРТЫВАНИЮ STEALTHNET
═══════════════════════════════════════════════════════════════════════════════


█ 1. ПОДГОТОВКА СЕРВЕРА (Ubuntu)
─────────────────────────────────────────────────────────────────────────────

▶ Обновление системы
  sudo apt update
  sudo apt upgrade -y

▶ Установка зависимостей
  sudo apt install -y nginx python3-pip python3-venv nodejs npm

▶ Установка Certbot для SSL
  sudo apt install certbot python3-certbot-nginx

▶ Создание папок проекта
  sudo mkdir -p /var/www/stealthnet-api
  sudo mkdir -p /var/www/stealthnet-client


█ 2. БЭКЕНД (Python / Flask)
─────────────────────────────────────────────────────────────────────────────

▶ Переход в директорию
  cd /var/www/stealthnet-api

▶ Создание виртуального окружения (один раз)
  python3 -m venv venv

▶ ⚠️  АКТИВАЦИЯ окружения (делать ПЕРЕД работой с Python)
  source venv/bin/activate

▶ Выход из окружения
  deactivate

▶ Установка зависимостей
  pip3 install flask flask-cors requests flask-sqlalchemy flask-bcrypt pyjwt \ pip3 install flask-caching cryptography flask-mail flask-limiter python-dotenv gunicorn

▶ 🆕 ИНИЦИАЛИЗАЦИЯ БАЗЫ ДАННЫХ (для новых установок)
  # Создает чистую базу данных со всеми таблицами и дефолтными настройками
  python3 init_db.py
  
  # После инициализации создайте администратора:
  python3 -m flask --app app make-admin nurmaga908@gmail.com

▶ Запуск сервера локально (видны логи в консоли)
  python3 app.py

▶ ⚠️  ЗАПУСК Gunicorn в фоновом режиме (продакшн)
  gunicorn --workers 3 --bind 127.0.0.1:5000 app:app --daemon

▶ ⚠️  ПЕРЕЗАГРУЗКА Бэкенда (после изменения кода)
  pkill gunicorn
  # затем снова запустить Gunicorn

▶ ⚠️  ОСВОБОЖДЕНИЕ ПОРТА 5000 (если занят другим процессом)
  
  # Способ 1: Использовать ss (обычно установлен по умолчанию)
  sudo ss -tulpn | grep :5000
  
  # Способ 2: Использовать netstat (если установлен)
  sudo netstat -tulpn | grep :5000
  
  # Способ 3: Установить lsof (если нужен)
  sudo apt install lsof -y
  sudo lsof -i :5000
  
  # После того как найдете PID процесса, остановите его:
  sudo kill -9 PID
  
  # Или остановить все процессы Gunicorn сразу:
  sudo pkill -9 gunicorn
  
  # Проверить, что порт освобожден:
  sudo ss -tulpn | grep :5000
  # Если ничего не выводится - порт свободен


█ 3. УПРАВЛЕНИЕ БАЗОЙ ДАННЫХ И АДМИНКОЙ
─────────────────────────────────────────────────────────────────────────────

▶ 🆕 ИНИЦИАЛИЗАЦИЯ ЧИСТОЙ БАЗЫ ДАННЫХ (для новых пользователей)
  # Этот скрипт создаст все таблицы и инициализирует дефолтные настройки
  python3 init_db.py
  
  # Скрипт автоматически:
  # - Создаст все таблицы с нуля
  # - Инициализирует SystemSetting (язык: ru, валюта: uah)
  # - Инициализирует ReferralSetting (бонус: 7 дней)
  # - Инициализирует PaymentSetting (id: 1)
  # - Инициализирует TariffFeatureSetting (Basic, Pro, Elite)
  
  # После инициализации создайте администратора:
  python3 -m flask --app app make-admin ВАШ_EMAIL

▶ Полный сброс БД (осторожно!)
  rm -rf instance

▶ Создание аккаунта администратора
  python3 -m flask --app app make-admin ВАШ_EMAIL

▶ Удаление неверифицированных аккаунтов
  python3 -m flask --app app clean-unverified

▶ Ручное создание БД через консоль Python (устаревший способ)
  python3 -m flask shell
  
  # Внутри интерпретатора:
  >>> from app import db
  >>> db.create_all()
  >>> exit()
  
  ⚠️  Рекомендуется использовать init_db.py вместо ручного создания


█ 4. ФРОНТЕНД (React)
─────────────────────────────────────────────────────────────────────────────

▶ Переход в директорию
  cd /var/www/stealthnet-client

▶ Установка зависимостей (с флагом совместимости)
  npm install --legacy-peer-deps

▶ Установка пакета иконок
  npm install react-icons --legacy-peer-deps

▶ Исправление прав доступа (если есть ошибка "Permission denied")
  chmod +x node_modules/.bin/react-scripts

▶ Запуск в режиме разработки (локальный ПК)
  npm start

▶ ⚠️  СБОРКА для продакшена (создает папку build/)
  npm run build


█ 5. ИНТЕГРАЦИЯ С TELEGRAM БОТОМ
─────────────────────────────────────────────────────────────────────────────

▶ Настройка переменных окружения для интеграции с ботом
  Добавьте в .env файл следующие переменные:
  
  # URL веб-API бота (без слэша в конце! например, https://bot.stealthnet.app)
  BOT_API_URL=https://bot.stealthnet.app
  
  # Токен для доступа к API бота (НЕ Telegram Bot Token!)
  # Это токен из веб-API бота, полученный через эндпоинт /tokens
  # См. инструкцию ниже как получить токен
  BOT_API_TOKEN=your-bot-api-token
  
  # Имя Telegram бота для Login Widget (без @)
  TELEGRAM_BOT_NAME=YourBotName

▶ ⚠️  ВАЖНО: Как получить токен для API бота
  Токен BOT_API_TOKEN - это НЕ Telegram Bot Token, а токен из веб-API бота!
  
  Способ 1: Использовать бутстрап-токен (если настроен)
  Если в настройках бота указан WEB_API_DEFAULT_TOKEN, он автоматически создается.
  Используйте его как BOT_API_TOKEN.
  
  Способ 2: Создать новый токен через API бота
  1. Получите бутстрап-токен или существующий токен из настроек бота
  2. Создайте новый токен через API:
  
     curl -X POST "https://bot.stealthnet.app/tokens" \
          -H "X-API-Key: YOUR_BOOTSTRAP_TOKEN" \
          -H "Content-Type: application/json" \
          -d '{"name": "Web Panel Integration", "description": "Token for web panel"}'
  
     Ответ будет содержать поле "token" - это и есть ваш BOT_API_TOKEN
     ⚠️  ВАЖНО: Токен показывается только один раз! Сохраните его сразу.
  
  Способ 3: Получить список существующих токенов
     curl -X GET "https://bot.stealthnet.app/tokens" \
          -H "X-API-Key: YOUR_BOOTSTRAP_TOKEN"
  
  ⚠️  НЕ путайте:
  - Telegram Bot Token (123456789:ABCdefGHIjklMNOpqrsTUVwxyz) - для работы Telegram Bot API
  - Web API Token - для работы с веб-API бота (получается через /tokens)

▶ Проверка токена API бота
  После настройки токена, проверьте его работу:
  
  curl -X GET "https://bot.stealthnet.app/health" \
       -H "X-API-Key: YOUR_BOT_API_TOKEN"
  
  Или:
  
  curl -X GET "https://bot.stealthnet.app/health" \
       -H "Authorization: Bearer YOUR_BOT_API_TOKEN"
  
  Должен вернуться ответ 200 с информацией о статусе API.
  
  Если получаете 401, проверьте:
  - Правильность токена (без пробелов, без кавычек)
  - Что токен активен (не отозван)
  - Формат заголовка (X-API-Key или Authorization: Bearer)

▶ Синхронизация пользователей из бота
  После настройки переменных окружения, можно синхронизировать пользователей:
  
  # Через админ-панель: Настройки → Синхронизация → Синхронизировать пользователей из бота
  # Или через API:
  curl -X POST "https://panel.stealthnet.app/api/admin/sync-bot-users" \
       -H "Authorization: Bearer YOUR_ADMIN_TOKEN"

▶ Авторизация через Telegram на веб-сайте
  Пользователи, зарегистрированные через бота, могут войти на сайт через Telegram:
  
  1. На странице входа появится кнопка "Войти через Telegram"
  2. Пользователь нажимает кнопку и авторизуется через Telegram
  3. Система автоматически найдет пользователя в базе бота и создаст/обновит запись в веб-панели
  4. Пользователь получает доступ к своим данным из RemnaWave

▶ Настройка Telegram Login Widget на фронтенде
  В файле admin-panel/.env (или admin-panel/.env.production) добавьте:
  
  REACT_APP_TELEGRAM_BOT_NAME=YourBotName
  
  После этого пересоберите фронтенд:
  cd /var/www/stealthnet-client
  npm run build

▶ ⚠️  ВАЖНО: Настройка домена для Telegram Login Widget
  Чтобы Telegram Login Widget работал, нужно добавить домен в настройки бота:
  
  1. Откройте @BotFather в Telegram
  2. Отправьте команду /mybots
  3. Выберите вашего бота
  4. Выберите "Bot Settings" (Настройки бота)
  5. Выберите "Domain" (Домен)
  6. Укажите ваш домен (например: panel.stealthnet.app)
     ⚠️  Важно: указывайте домен БЕЗ https:// и БЕЗ слэша в конце
     ✅ Правильно: panel.stealthnet.app
     ❌ Неправильно: https://panel.stealthnet.app
     ❌ Неправильно: panel.stealthnet.app/
  
  7. Подтвердите добавление домена
  
  После настройки домена перезагрузите страницу в браузере.
  
  Если вы видите ошибку "Bot domain invalid", проверьте:
  - Правильность имени бота (REACT_APP_TELEGRAM_BOT_NAME)
  - Что домен добавлен в @BotFather (без https://)
  - Что домен совпадает с текущим доменом сайта
  - Что сайт доступен по HTTPS (Telegram требует HTTPS)


█ 6. НАСТРОЙКА ПЛАТЕЖНЫХ СИСТЕМ
─────────────────────────────────────────────────────────────────────────────

▶ Telegram Stars Webhook
  После настройки Telegram Bot Token в админ-панели, необходимо установить webhook:
  
  curl -X POST "https://api.telegram.org/botВАШ_BOT_TOKEN/setWebhook" \
       -H "Content-Type: application/json" \
       -d '{
         "url": "https://panel.stealthnet.app/api/webhook/telegram",
         "allowed_updates": ["pre_checkout_query", "message"]
       }'
  
  ⚠️  ВАЖНО: Замените ВАШ_BOT_TOKEN на реальный токен бота из админ-панели
  
  ▶ ПРОВЕРКА СТАТУСА WEBHOOK:
  
  Способ 1: Через API Telegram (прямая проверка)
  curl "https://api.telegram.org/botВАШ_BOT_TOKEN/getWebhookInfo"
  
  Ожидаемый ответ при успешной настройке:
  {
    "ok": true,
    "result": {
      "url": "https://panel.stealthnet.app/api/webhook/telegram",
      "has_custom_certificate": false,
      "pending_update_count": 0,
      "last_error_date": null,
      "last_error_message": null
    }
  }
  
  ⚠️  Если pending_update_count > 0 - есть необработанные обновления
  ⚠️  Если last_error_message не null - есть проблемы с webhook
  
  Способ 2: Через админ-панель (требуется авторизация)
  GET https://panel.stealthnet.app/api/admin/telegram-webhook-status
  Headers: Authorization: Bearer АДМИН_ТОКЕН
  
  Способ 3: Тестовая оплата
  1. Создайте тестовый платеж через Telegram Stars в клиентской панели
  2. Проверьте логи сервера на наличие сообщений:
     - "Telegram PreCheckoutQuery received: order_id=..."
     - "Telegram PreCheckoutQuery confirmed successfully"
     - "Telegram successful payment received: order_id=..."
  
  Способ 4: Проверка логов сервера
  # Просмотр логов в реальном времени (если используете systemd):
  sudo journalctl -u ваш_сервис -f
  
  # Или если запускаете через gunicorn напрямую:
  # Логи будут в stdout/stderr процесса
  
  ▶ АВТОМАТИЧЕСКАЯ УСТАНОВКА WEBHOOK:
  POST https://panel.stealthnet.app/api/admin/telegram-set-webhook
  Headers: Authorization: Bearer АДМИН_ТОКЕН

▶ Тестирование Heleket Webhook
  Используйте документацию Heleket для тестирования webhook:
  https://doc.heleket.com/ru/methods/payments/testing-webhook
  
  Пример тестового запроса (требуется авторизация админа):
  curl -X POST "https://panel.stealthnet.app/api/admin/test-heleket-webhook" \
       -H "Authorization: Bearer АДМИН_ТОКЕН" \
       -H "Content-Type: application/json" \
       -d '{
         "currency": "USDT",
         "network": "bsc",
         "status": "paid",
         "order_id": "test-order-123"
       }'


█ 7. NGINX И SSL (Веб-сервер)
─────────────────────────────────────────────────────────────────────────────

▶ Редактирование конфигурации Nginx
  sudo nano /etc/nginx/sites-available/default

▶ Проверка конфига на ошибки
  sudo nginx -t

▶ Перезагрузка Nginx (применить изменения)
  sudo systemctl restart nginx

▶ Получение SSL сертификата (HTTPS)
  sudo certbot --nginx -d panel.stealthnet.app


═══════════════════════════════════════════════════════════════════════════════




   