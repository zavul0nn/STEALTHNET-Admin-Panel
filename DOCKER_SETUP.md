# Docker Setup для StealthNET

Это руководство поможет вам запустить StealthNET в Docker контейнерах с использованием Nginx или Caddy.

## 📋 Требования

- Docker 20.10+
- Docker Compose 2.0+
- Минимум 2GB RAM
- Минимум 5GB свободного места

## 🚀 Быстрый старт

### 1. Подготовка

```bash
# Клонируйте или скопируйте проект
cd "Готовый проект"

# Создайте файл .env на основе .env.example
cp .env.example .env

# Отредактируйте .env и заполните все необходимые переменные
nano .env
```

### 2. Создание необходимых директорий

```bash
mkdir -p data instance templates nginx-logs
```

### 3. Запуск с Nginx

```bash
# Запуск всех сервисов (API + Nginx)
docker-compose up -d

# Просмотр логов
docker-compose logs -f

# Остановка
docker-compose down
```

### 4. Запуск с Caddy (альтернатива)

```bash
# Запуск с профилем Caddy (вместо Nginx)
docker-compose --profile caddy up -d caddy api

# Просмотр логов
docker-compose logs -f caddy

# Остановка
docker-compose --profile caddy down
```

## 🔧 Настройка Nginx

### Базовая конфигурация (HTTP)

Конфигурация уже настроена в `nginx.conf` для работы по HTTP на порту 80.

### Настройка SSL (HTTPS)

1. **Получите SSL сертификаты** (например, через Let's Encrypt):

```bash
# Установите certbot
sudo apt-get install certbot

# Получите сертификаты
sudo certbot certonly --standalone -d your-domain.com

# Скопируйте сертификаты
sudo cp /etc/letsencrypt/live/your-domain.com/fullchain.pem ./ssl/cert.pem
sudo cp /etc/letsencrypt/live/your-domain.com/privkey.pem ./ssl/key.pem
```

2. **Обновите nginx.conf**:

Раскомментируйте блок HTTPS сервера в `nginx.conf` и замените `your-domain.com` на ваш домен.

3. **Смонтируйте сертификаты в docker-compose.yml**:

```yaml
nginx:
  volumes:
    - ./ssl:/etc/nginx/ssl:ro
```

4. **Перезапустите Nginx**:

```bash
docker-compose restart nginx
```

### Настройка домена

1. Отредактируйте `nginx.conf`
2. Замените `server_name _;` на `server_name your-domain.com;`
3. Перезапустите контейнер:

```bash
docker-compose restart nginx
```

## 🔧 Настройка Caddy

Caddy автоматически получает SSL сертификаты через Let's Encrypt.

### Настройка с доменом

1. Отредактируйте `Caddyfile`
2. Раскомментируйте блок с доменом и замените `your-domain.com`:

```caddy
your-domain.com {
    # ... остальная конфигурация
}
```

3. Убедитесь, что DNS записи указывают на ваш сервер:

```bash
# A запись
your-domain.com    A    1.2.3.4

# Или AAAA для IPv6
your-domain.com    AAAA    2001:db8::1
```

4. Запустите Caddy:

```bash
docker-compose --profile caddy up -d caddy api
```

### Настройка без домена (HTTP)

Используйте вариант 2 в `Caddyfile` (уже настроен по умолчанию).

## 📁 Структура проекта

```
Готовый проект/
├── app.py                 # Flask приложение
├── requirements.txt        # Python зависимости
├── Dockerfile             # Docker образ для API
├── docker-compose.yml     # Docker Compose конфигурация
├── nginx.conf             # Конфигурация Nginx
├── Caddyfile              # Конфигурация Caddy
├── .env                   # Переменные окружения (создайте из .env.example)
├── .dockerignore          # Исключения для Docker
├── data/                  # Данные приложения (создается автоматически)
├── instance/              # Instance папка Flask (создается автоматически)
├── templates/             # HTML шаблоны (если есть)
└── frontend/
    └── build/             # Собранный React фронтенд
```

## 🔐 Переменные окружения

Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
nano .env
```

**Обязательные переменные:**
- `ADMIN_TOKEN` - токен для доступа к RemnaWave API
- `API_URL` - URL RemnaWave API
- `DEFAULT_SQUAD_ID` - ID дефолтного сквада
- `YOUR_SERVER_IP` - ваш домен или IP (например, `https://panel.stealthnet.app`)
- `JWT_SECRET_KEY` - секретный ключ для JWT токенов
- `FERNET_KEY` - ключ для шифрования (сгенерируйте через `python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"`)

**Опциональные:**
- `BOT_API_URL` - URL API Telegram бота
- `BOT_API_TOKEN` - токен для API бота
- `TELEGRAM_BOT_NAME` - имя Telegram бота
- `MAIL_*` - настройки почты

## 🛠️ Полезные команды

### Просмотр логов

```bash
# Все сервисы
docker-compose logs -f

# Только API
docker-compose logs -f api

# Только Nginx
docker-compose logs -f nginx

# Только Caddy
docker-compose logs -f caddy
```

### Перезапуск сервисов

```bash
# Перезапуск всех
docker-compose restart

# Перезапуск конкретного сервиса
docker-compose restart api
docker-compose restart nginx
```

### Обновление приложения

```bash
# Остановка
docker-compose down

# Пересборка образов
docker-compose build --no-cache

# Запуск
docker-compose up -d
```

### Создание администратора

```bash
# Войдите в контейнер API
docker-compose exec api bash

# Создайте администратора
python -m flask --app app make-admin your-email@example.com

# Выйдите
exit
```

### Резервное копирование базы данных

```bash
# Копирование базы данных из контейнера
docker-compose exec api cp /app/data/stealthnet.db /app/backup_$(date +%Y%m%d).db

# Или через volume
cp data/stealthnet.db backup_$(date +%Y%m%d).db
```

## 🌐 Порты

- **80** - HTTP (Nginx/Caddy)
- **443** - HTTPS (Nginx/Caddy)
- **5000** - Flask API (внутренний, не открывайте наружу)

## 🔒 Безопасность

1. **Не открывайте порт 5000 наружу** - API должен быть доступен только через Nginx/Caddy
2. **Используйте HTTPS в продакшене** - настройте SSL сертификаты
3. **Регулярно обновляйте зависимости** - проверяйте `requirements.txt`
4. **Используйте сильные пароли** - для JWT_SECRET_KEY и FERNET_KEY
5. **Ограничьте доступ к .env** - файл должен быть доступен только владельцу

## 🐛 Решение проблем

### Проблема: API не запускается

```bash
# Проверьте логи
docker-compose logs api

# Проверьте переменные окружения
docker-compose exec api env | grep -E "ADMIN_TOKEN|API_URL"

# Проверьте подключение к базе данных
docker-compose exec api ls -la /app/data/
```

### Проблема: Nginx/Caddy не проксирует API

1. Проверьте, что API запущен: `docker-compose ps`
2. Проверьте конфигурацию: `docker-compose exec nginx nginx -t`
3. Проверьте логи: `docker-compose logs nginx`

### Проблема: Фронтенд не загружается

1. Убедитесь, что `frontend/build` содержит собранные файлы
2. Проверьте права доступа: `ls -la frontend/build/`
3. Проверьте логи Nginx/Caddy

### Проблема: SSL сертификаты не работают

**Для Nginx:**
- Убедитесь, что сертификаты находятся в `./ssl/`
- Проверьте права доступа: `chmod 644 ssl/*.pem`
- Проверьте конфигурацию в `nginx.conf`

**Для Caddy:**
- Убедитесь, что DNS записи настроены правильно
- Проверьте, что порты 80 и 443 открыты в firewall
- Проверьте логи: `docker-compose logs caddy`

## 📚 Дополнительные ресурсы

- [Docker Documentation](https://docs.docker.com/)
- [Docker Compose Documentation](https://docs.docker.com/compose/)
- [Nginx Documentation](https://nginx.org/en/docs/)
- [Caddy Documentation](https://caddyserver.com/docs/)

## 💡 Советы

1. **Используйте Caddy для автоматического SSL** - проще настройка
2. **Используйте Nginx для большей гибкости** - больше опций конфигурации
3. **Настройте мониторинг** - используйте healthcheck в docker-compose
4. **Регулярно делайте бэкапы** - особенно базы данных
5. **Используйте reverse proxy** - не открывайте API напрямую

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи: `docker-compose logs -f`
2. Проверьте статус контейнеров: `docker-compose ps`
3. Проверьте конфигурацию: `docker-compose config`

